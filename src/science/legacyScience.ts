/**
 * Valeur conservée uniquement pour la démonstration 2D historique de Phase 8.
 * Elle n'est jamais utilisée par le sampler, le champ ou le rendu orbital 3D.
 * Le modèle photo-interaction sera remplacé et décrit dans cette phase
 * ultérieure ; il est donc volontairement isolé derrière ce nom explicite.
 */
export const LEGACY_PHOTOELECTRIC_ENERGY = -13.6 / 4 - -13.6;
